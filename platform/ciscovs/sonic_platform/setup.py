#!/usr/bin/env python

import os
from setuptools import setup

setup(
   name = 'sonic_platform',
   version = '1.0',

   packages = ['sonic_platform'],
   package_dir = {'sonic_platform': 'sonic_platform'},
)
